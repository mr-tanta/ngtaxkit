{
  "name": "ngtaxkit",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/typescript",
    "packages/python"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "npm@10.8.0",
  "engines": {
    "node": ">=18"
  }
}
