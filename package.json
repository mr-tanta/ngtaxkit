{
  "name": "ngtaxkit-workspace",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/typescript",
    "packages/python"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run build && npm run test --workspaces --if-present",
    "lint": "npm run build && npm run lint --workspaces --if-present",
    "type-check": "npm run build && npm run type-check --workspaces --if-present",
    "verify:rates": "node scripts/verify-rate-data-sync.mjs",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "packageManager": "npm@10.8.0",
  "engines": {
    "node": ">=18"
  }
}
